# PHP `DateTime` 可変性デバッグラボ

`DateTime::modify()` が同じオブジェクトを変更するため、支払期限を計算したつもりで請求日まで変わってしまう挙動を再現する。

| 項目 | 内容 |
|---|---|
| PHP | 8.3.6 CLI |
| 外部依存 | なし |
| 再現コマンド | `php tests/run.php` |

初期コミットでは、請求日が発行日のまま残るべきというテストが失敗する。修正後は `DateTimeImmutable` を用い、元の日時を変更しない契約を回帰テストで固定する。
